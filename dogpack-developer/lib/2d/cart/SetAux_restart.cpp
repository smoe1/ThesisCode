//#include "tensors.h"
//#include <string>
//using namespace std;

// deprecated
#if 0
void SetAux_restart(
    int nstart,
    dTensorBC4& aux,
    const char* outputdir)
{
    double ReadStateArray(const char* dir, const char* varname,
        dTensorBC4& q, int nstart);
    ReadStateArray(outputdir, "a", aux, nstart);
}
#endif
